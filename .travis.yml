language: ruby
rvm:
  - 2.6.3

script: ./.ci/travis.sh

sudo: false

env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

before_install:
  - gem update --system
  - gem install bundler

cache:
  directories:
    - venv
    - vendor
